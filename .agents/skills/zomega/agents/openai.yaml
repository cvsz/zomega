interface:
  display_name: "Zomega"
  short_description: "Repo-specific patterns and workflows for zomega"
  default_prompt: "Use the zomega repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true