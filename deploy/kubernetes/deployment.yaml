apiVersion: apps/v1
kind: Deployment
metadata:
  name: omega-api
  namespace: omega
spec:
  replicas: 2
  selector: {matchLabels: {app: omega-api}}
  template:
    metadata: {labels: {app: omega-api}}
    spec:
      serviceAccountName: omega
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: {app: omega-api}
      containers:
        - name: api
          image: ghcr.io/cvsz/zomega:3.0.0
          imagePullPolicy: IfNotPresent
          command: ["python","-m","omega","serve"]
          ports: [{name: http, containerPort: 8000}]
          envFrom: [{secretRef: {name: omega-runtime}}]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsUser: 10001
            capabilities: {drop: ["ALL"]}
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
          readinessProbe:
            httpGet: {path: /health/ready, port: http}
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet: {path: /health/live, port: http}
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests: {cpu: "200m", memory: "256Mi"}
            limits: {cpu: "2", memory: "2Gi"}
      volumes:
        - name: tmp
          emptyDir: {}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: omega-worker
  namespace: omega
spec:
  replicas: 2
  selector: {matchLabels: {app: omega-worker}}
  template:
    metadata: {labels: {app: omega-worker}}
    spec:
      serviceAccountName: omega
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile: {type: RuntimeDefault}
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels: {app: omega-worker}
      containers:
        - name: worker
          image: ghcr.io/cvsz/zomega:3.0.0
          imagePullPolicy: IfNotPresent
          command: ["arq","omega.jobs.WorkerSettings"]
          envFrom: [{secretRef: {name: omega-runtime}}]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsUser: 10001
            capabilities: {drop: ["ALL"]}
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
          resources:
            requests: {cpu: "200m", memory: "256Mi"}
            limits: {cpu: "2", memory: "2Gi"}
      volumes:
        - name: tmp
          emptyDir: {}
