apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-zomega
spec:
  selector: {app.kubernetes.io/name: zomega-api}
  ports:
    - {name: http, port: {{ .Values.service.port }}, targetPort: http}
