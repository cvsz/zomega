name: Deploy
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  deploy:
    environment: production
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Connect deployment target here; secrets must come from GitHub Environment Secrets."
